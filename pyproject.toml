[project]
name = "bootdev-webscraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp==3.12.12",
    "beautifulsoup4==4.13.4",
    "requests==2.32.4",
]


[tool.black]
line-length = 88
# Black will format files in-place. Configure any additional options here.

[tool.isort]
profile = "black"
